# Audio Dictation - Помічник для вивчення англійської

Розділяє аудіо на фрагменти фраз і повторює їх для диктанту.

## Особливості

- 🎯 Використовує **Whisper AI** для точного визначення фраз по реченнях
- 📝 Розпізнає текст і створює транскрипцію з таймінгами
- 🔄 Повторює кожну фразу N разів з паузами
- 🎵 Зберігає якість оригінального аудіо
- 🌍 Працює повністю локально (не потрібен інтернет)

## Встановлення

### 1. Node.js залежності
```bash
npm install
```

### 2. Python залежності

Створіть віртуальне середовище та встановіть залежності:

```bash
python3 -m venv venv
./venv/bin/pip install -r requirements.txt
```

або окремо:
```bash
python3 -m venv venv
./venv/bin/pip install openai-whisper
```

### 3. ffmpeg
Переконайтеся що ffmpeg встановлений:
```bash
ffmpeg -version
```

## Структура проєкту

```
audio-source/              # Розміщуйте тут вхідні аудіо файли
result/
  ├── audio/              # Згенеровані аудіо для диктанту (з повтореннями)
  ├── shadowing/          # Згенеровані аудіо для shadowing (сегмент + пауза)
  └── text/               # Згенеровані транскрипти з таймінгами
```

## Використання

1. Покладіть ваш аудіо файл (.mp3) в папку `audio-source/`
   - **Останній змінений файл** буде вибрано автоматично
2. Налаштуйте `config.json` (опціонально):
   ```json
   {
     "whisperPrompt": "Опис змісту відео",
     "repeatCount": 2,
     "pauseBetweenRepeats": 3
   }
   ```
3. Запустіть:
   ```bash
   make run
   ```

Щоб очистити всі згенеровані файли:
```bash
make clear
```

Вихідні файли автоматично іменуються з датою + номером:
- `result/audio/output_dictation_20250208_0001.mp3` - **Диктант**: фрази повторюються кілька разів з паузами
- `result/shadowing/output_dictation_20250208_0001.mp3` - **Shadowing**: кожна фраза + тиша (така ж тривалість) для практики
- `result/text/transcript_20250208_0001.txt` - текст з таймінгами кожного речення

Наступний запуск сьогодні створить `20250208_0002`, завтра почнеться з `20250209_0001`.

## Налаштування

Відредагуйте `config.json`:

```json
{
  "whisperPrompt": "Steven looks at a picture of a big red bus and talks about it.",
  "repeatCount": 2,
  "pauseBetweenRepeats": 3,
  "pauseAfterSegment": 10,
  "minSegmentLength": 0.4,
  "device": "cuda"
}
```

**Параметри:**
- `whisperPrompt` - Опис відео (допомагає Whisper краще розпізнавати, залиште пустим якщо невідомо)
- `repeatCount` - Скільки разів повторювати фразу (за замовчуванням: 2)
- `pauseBetweenRepeats` - Коротка пауза між повтореннями в секундах (за замовчуванням: 3)
- `pauseAfterSegment` - Довга пауза після всіх повторів, перед наступною фразою (за замовчуванням: 10)
- `minSegmentLength` - Мінімальна довжина сегмента для фільтрації шуму (за замовчуванням: 0.4)
- `device` - Пристрій обробки: `"cuda"` для GPU (швидко), `"cpu"` для CPU (повільно) (за замовчуванням: "cuda")

**Примітка:**
- Аудіо файл вибирається автоматично (останній .mp3 з `audio-source/`)
- Змінюйте `whisperPrompt` для кращого розпізнавання специфічних слів!

## Як це працює

1. **Whisper AI**: Розпізнає мовлення і знаходить таймкоди кожного речення
2. **Transcript**: Зберігає текст з таймінгами в `transcript.txt`
3. **Splitting**: Ділить аудіо на фрагменти за таймкодами від Whisper
4. **Assembly**: Збирає фінальний файл з повтореннями та паузами

## Приклад

```bash
# Обробити файл audio.mp3
node index.js audio.mp3

# Використовувати інший файл
node index.js lesson_01.mp3
```

---

[English version](README.md)
